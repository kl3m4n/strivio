// Réservé pour les types et zod schemas partagés entre web et Convex
// (validation des forms côté UI === validation des args côté backend).
// Vide pour l'instant — à remplir à la phase 2 (programs / days / blocks).
export {}
